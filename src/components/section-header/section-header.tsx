import { SearchBlock } from '../search-block';
import { SectionInfo } from '../section-info';

export const SectionHeader = () => {
    return (
        <>
            <SectionInfo />
            <SearchBlock />
        </>
    );
};
